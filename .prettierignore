**/routeTree.gen.ts

# Generated by apps/docs/scripts/gen-openapi.mjs and checked by a CI drift gate
# (regenerate, then `git diff --exit-code`). Prettier collapses short arrays that
# the generator writes expanded, so formatting this file makes the gate fail on
# every pull request.
apps/docs/src/openapi/tradr-api.json

# Astro-generated content-collection types
**/.astro/**

# The four reference PAGES are generated by apps/docs/scripts/gen-*.mjs and
# checked by the same CI drift gate (regenerate, then `git diff --exit-code`).
# The generators write raw markdown; prettier reflows it, so formatting these
# makes the gate fail on every pull request.
apps/docs/src/content/docs/self-hosting/reference/env-vars.mdx
apps/docs/src/content/docs/self-hosting/reference/cli.mdx
apps/docs/src/content/docs/self-hosting/reference/db-schema.mdx
apps/docs/src/content/docs/user-guide/reference/plan-limits.mdx

# The "How it's built" figures are generated by
# apps/docs/scripts/gen-how-its-built.mjs and checked by the same CI drift gate.
# The generator writes the ciJobs array expanded; prettier 3.8.1 collapses it, so
# formatting this file makes the gate fail on every pull request.
apps/docs/src/content/docs/self-hosting/explanation/_how-its-built.json
