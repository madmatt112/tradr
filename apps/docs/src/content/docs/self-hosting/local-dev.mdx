---
title: "Set up a local dev environment"
description: "The pnpm monorepo dev loop: install, dev Postgres via docker-compose.dev.yml, pnpm dev, seed data, running tests, and Mailpit for local email."
pagefind: false
head:
  - tag: meta
    attrs:
      name: robots
      content: noindex
---

The pnpm monorepo dev loop: install, dev Postgres via docker-compose.dev.yml, pnpm dev, seed data, running tests, and Mailpit for local email.

:::note
This page is not written yet. It is listed so the shape of the documentation is
visible. It stays out of search and out of search-engine indexes until it has
content.
:::
