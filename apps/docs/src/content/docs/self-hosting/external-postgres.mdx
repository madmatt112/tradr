---
title: "Bring your own Postgres / run behind a pooler"
description: "Point at an external database, and use DIRECT_DATABASE_URL and DB_TRANSACTION_POOLER for transaction-mode poolers (migrations need a direct connection)."
pagefind: false
head:
  - tag: meta
    attrs:
      name: robots
      content: noindex
---

Point at an external database, and use DIRECT_DATABASE_URL and DB_TRANSACTION_POOLER for transaction-mode poolers (migrations need a direct connection).

:::note
This page is not written yet. It is listed so the shape of the documentation is
visible. It stays out of search and out of search-engine indexes until it has
content.
:::
