---
title: "Back up and restore"
description: "pg_dump logical dumps and pgdata volume copies, restoring into a fresh stack, and where data lives (the Postgres named volume)."
pagefind: false
head:
  - tag: meta
    attrs:
      name: robots
      content: noindex
---

pg_dump logical dumps and pgdata volume copies, restoring into a fresh stack, and where data lives (the Postgres named volume).

:::note
This page is not written yet. It is listed so the shape of the documentation is
visible. It stays out of search and out of search-engine indexes until it has
content.
:::
