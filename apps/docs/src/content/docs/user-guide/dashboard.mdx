---
title: "Customise the dashboard"
description: "Add, remove, and drag-drop widgets — stats, open positions, equity curve, account balances, position sizing — with a layout that persists per user."
pagefind: false
head:
  - tag: meta
    attrs:
      name: robots
      content: noindex
---

Add, remove, and drag-drop widgets — stats, open positions, equity curve, account balances, position sizing — with a layout that persists per user.

:::note
This page is not written yet. It is listed so the shape of the documentation is
visible. It stays out of search and out of search-engine indexes until it has
content.
:::
